name: Update DEB versions

on:
  schedule:
    - cron: '0 * * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: Install dependencies
        run: pip install requests

      - name: Fetch and generate versions.json
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: python update_versions.py apps.json

      - name: Check for changes
        id: check_changes
        run: |
          # Check if file is modified (tracked) or new (untracked)
          if git status --porcelain versions.json | grep -q '^[ MADRC]'; then
            echo "changed=true" >> $GITHUB_OUTPUT
            echo "File versions.json has changes"
          else
            echo "changed=false" >> $GITHUB_OUTPUT
            echo "No changes to versions.json"
          fi

      - name: Commit and push if changed
        if: steps.check_changes.outputs.changed == 'true'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add versions.json
          git commit -m "Update DEB versions: $(date -u +'%Y-%m-%d %H:%M:%S')"
          git push
