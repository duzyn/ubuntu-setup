name: Update Package Versions

on:
  schedule:
    - cron: '0 * * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: Install dependencies
        run: pip install requests

      - name: Fetch and generate versions
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: python update_versions.py apps.json

      - name: Check for changes
        id: check_changes
        run: |
          status=$(git status --porcelain versions.json 2>/dev/null || true)
          echo "Status: '$status'"
          
          if [ -n "$status" ]; then
            echo "changed=true" >> $GITHUB_OUTPUT
            echo "versions.json has changes"
          else
            echo "changed=false" >> $GITHUB_OUTPUT
            echo "No changes detected"
          fi

      - name: Commit and push if changed
        if: steps.check_changes.outputs.changed == 'true'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add versions.json
          git commit -m "Update package versions: $(date -u +'%Y-%m-%d %H:%M:%S')"
          git push
